# Human-edited inputs — lower-bound (`>=`) pins only; a regression test enforces
# that (tests/test_dependency_lock.py). The exact resolution lives in
# requirements.lock (used by setup and CI); regenerate it per
# docs/dev-workflows.md#python-dependency-updates after editing this file.
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.0.0
pydantic>=2.13.5
pydantic-settings>=2.7.0
boto3>=1.35.0

# The MuJoCo Playground rollout + render engine (jax, mujoco, mujoco-mjx,
# numpy, imageio) is NOT here on purpose — it is a heavy, platform-specific
# stack that would make the baseline install slow and fragile. It lives in the
# gated requirements-ml.txt, is lazy-imported in app/service/mujoco_engine.py,
# and a run degrades to an actionable error when it is absent. See
# docs/features/policy-rollout.md.

# Dev / quality
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
